# Production environment variables
# Actual values must be set in Netlify dashboard (Site settings > Environment variables)
# Do NOT hardcode real keys here

VITE_STRIPE_PUBLIC_KEY=${VITE_STRIPE_PUBLIC_KEY}
